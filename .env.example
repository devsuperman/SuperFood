# Copy to .env to override the docker-compose defaults (all optional —
# docker compose up works out of the box without this file).

JWT_SIGNING_KEY=local-docker-dev-signing-key-do-not-use-in-production
PLATFORM_ADMIN_EMAIL=admin@superfood.dev
PLATFORM_ADMIN_PASSWORD=Passw0rd!Admin
